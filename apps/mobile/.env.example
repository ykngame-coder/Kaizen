# Supabase (public keys — safe to ship in the client).
# Local stack: run `pnpm db:start` at the repo root, then copy the printed values.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key
