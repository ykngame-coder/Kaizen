import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function TrainingTab(): React.JSX.Element {
  return (
    <PlaceholderScreen
      title="Entraînement"
      subtitle="Programmes, séances, bibliothèque d'exercices et progression."
      comingIn="Création de séance, bibliothèque d'exercices et mode séance active — Étape 3."
    />
  );
}
